{
  "name": "seoreact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build1": "browserify src/index.js -o dist/index.js",
    "build2": "browserify src/index2.js -o dist/index2.js",
    "build3": "browserify src/index3.js -o dist/index3.js",
    "build4": "browserify src/arrayfill.js -o dist/arrayfill.js",
    "build5": "browserify src/arrayapply.js -o dist/arrayapply.js",
    "build6": "browserify src/arraykeys.js -o dist/arraykeys.js",
    "build7": "browserify src/poly.js -o dist/poly.js",
    "buildpol": "browserify src/polyfill.js -o dist/polyfill.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-runtime": "^6.6.1",
    "react": "^0.14.8",
    "react-dom": "^0.14.8"
  },
  "devDependencies": {
    "babel-plugin-transform-runtime": "^6.6.0",
    "babel-polyfill": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-3": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-3"
    ],
    "plugins": [
      "transform-runtime"
    ]
  }
}
